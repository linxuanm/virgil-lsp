def main() -> int {
  var log = System.fileOpen("log.txt", false);
  System.write(log, "Program started\n");
  
  while (true) {
    var s = Transport.readline(0);
    System.write(log, Strings.format1("%s\n", s));
  }
  return 0;
}

def main() -> int {
  var log = System.fileOpen("new-log.txt", false);
  System.write(log, "Program started\n");
  
  var p = PacketParser.new();
	var res = p.parse(0);
	var out = if(p.ok, res, p.err_msg);

	System.write(log, Strings.format1("%s\n", out));

  return 0;
}
